# Environment 
MKDIR=mkdir
CP=cp
CCADMIN=CCadmin
CXX=g++
CXXFLAGS=--std=c++14 -Wall -Werror -Wextra -Wpedantic -O0 -ggdb
# CXXFLAGS=--std=c++14 -Wall -Werror -Wextra -Wpedantic -O2 
BUILDDIR=build
OUT=$(BUILDDIR)/agsolver
SRC=src
GTEST=/opt/gtest

# directory to be user for libraries compiled as dependencies for solver application
LIBDIR=/tmp/libs